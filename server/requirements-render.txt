# Render Free candidate: YOLO11n CPU inference only.
# Keep PyTorch on the CPU wheel index so pip does not pull CUDA libraries.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi>=0.115,<1
uvicorn>=0.34,<1
python-multipart>=0.0.20,<1
python-dotenv>=1.0,<2
torch==2.6.0+cpu
torchvision==0.21.0+cpu
ultralytics>=8.3,<9
pillow>=11,<13
